# Commit and Push

Shortcut that runs `/commit` then `/push` in sequence.

## Steps

1. Follow all rules and steps from `/commit`
2. After a successful commit, follow all rules and steps from `/push`

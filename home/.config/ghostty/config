# Ghostty config. Snazzy palette.

# Font
font-family = "JetBrainsMono Nerd Font Mono"
font-style = "Regular"
font-style-bold = "Bold"
font-style-italic = "Italic"
font-style-bold-italic = "Bold Italic"
font-size = 12
font-thicken = true

# Initial window size (cells)
window-width = 120
window-height = 25

# Ligatures and stylistic sets
font-feature = calt
font-feature = liga
font-feature = dlig
font-feature = ss01
font-feature = ss02
font-feature = ss19
font-feature = zero

# Cursor
cursor-style = bar
cursor-style-blink = true

# Window, transparency, blur
background-opacity = 0.97
background-blur = 25
window-decoration = auto
window-padding-balance = true
macos-titlebar-style = transparent

# Bell. Visual only, no audio.
bell-features = attention,title,border

# Scrollback
scrollback-limit = 107374182400000

# Mouse
mouse-hide-while-typing = true
mouse-scroll-multiplier = precision:2.5,discrete:6

# Keyboard. Pass option through normally.
macos-option-as-alt = false

# Shell integration and clipboard
shell-integration = detect
shell-integration-features = ssh-terminfo,ssh-env
clipboard-read = allow
clipboard-write = allow

# Snazzy theme
theme = light:snazzy-soft-light,dark:Snazzy Soft

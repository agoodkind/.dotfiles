#!/usr/bin/env bash
# Migration stub — the Go rewrite (c6b71cf) replaced this with
# dotfilesctl dispatch. Old incl.zsh calls this on the first shell open
# after the transition sync. This file can be removed once all machines
# have updated past that commit.
exit 0

---
description: Ruby development standards
globs: "**/*.rb"
alwaysApply: false
---

# Ruby Development Standards

## Guard Clauses

Use one guard clause per condition:

```ruby
return foo unless bar
return bar if foo
return unless x
return unless y
return if z
```
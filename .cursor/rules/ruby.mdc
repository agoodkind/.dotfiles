---
description: 
globs: "**/*.rb"
alwaysApply: false
---

# Ruby Development Standards

## Guard Clause

Use one guard clause per condition:

```ruby
return foo unless bar
return bar if foo
return unless x
return unless y
return if z
```

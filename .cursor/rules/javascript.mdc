---
description: JavaScript and TypeScript code style conventions
globs: "**/*.{js,jsx,ts,tsx}"
alwaysApply: true
---

# JavaScript / TypeScript Style

- Use `function` definitions for top-level functions, not arrow functions.

```typescript
function handleClick(event) { ... } // ✅
const handleClick = (event) => { ... } // ❌
```

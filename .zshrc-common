
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

export ZSH="$HOME/.oh-my-zsh"

ZSH_THEME="powerlevel10k/powerlevel10k"
POWERLEVEL9K_DISABLE_GITSTATUS=true

common_plugins=(zsh-autosuggestions colored-man-pages zsh-navigation-tools git zsh-syntax-highlighting)


# source $ZSH/oh-my-zsh.sh # needs to run later so we get all the plugins

# . "$HOME/.local/bin/env"
# source ~/powerlevel10k/powerlevel10k.zsh-theme

# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
# [[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh # needs to run after source $ZSH/oh-my-zsh.sh

alias nano=vim
alias pbcopy="ssh mba pbcopy"
alias ls=ls -a
